pipeline {
         agent any
         stages {
                 stage('One') {
                 steps {
                     npm install
                 }
                 }
                 
              }
}